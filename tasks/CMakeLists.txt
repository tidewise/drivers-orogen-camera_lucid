# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(camera_lucidTaskLib)
ADD_LIBRARY(${CAMERA_LUCID_TASKLIB_NAME} SHARED
    ${CAMERA_LUCID_TASKLIB_SOURCES})
add_dependencies(${CAMERA_LUCID_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${CAMERA_LUCID_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${CAMERA_LUCID_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${CAMERA_LUCID_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${CAMERA_LUCID_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${CAMERA_LUCID_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${CAMERA_LUCID_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${CAMERA_LUCID_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${CAMERA_LUCID_TASKLIB_HEADERS}
    DESTINATION include/orocos/camera_lucid)

